# generate site-specific rate table

generate site-specific rate table

## Usage

``` r
filter_woncan(site)
```

## Arguments

- site:

  character(1) name of anatomic site, must reside in value of
  \`woncan_types()\`
